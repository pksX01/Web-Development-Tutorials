
    var v1 = 10;
    var v2 = 20;
    var v3 = 30;

    if(v1==v2 && v2==v3)
        document.write("All are same");
    else if(v1>v2 && v1>v3)
        document.write(v1+" is greatest.");
    else if(v2>v2 && v2>v3)
        document.write(v2+" is greatest.");
    else 
        document.write(v3+" is greatest.");
